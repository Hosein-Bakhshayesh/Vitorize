{
  "//": "Automated-test environment (Playwright E2E stack and integration fixtures). Allowed origins are declared here in full because the base appsettings.json intentionally ships an empty list; see the //Cors note there.",
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5077",
      "http://127.0.0.1:5077",
      "http://localhost:5177",
      "http://127.0.0.1:5177"
    ]
  }
}
