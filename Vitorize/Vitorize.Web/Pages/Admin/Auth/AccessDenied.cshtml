@page

@{
    Layout = "_AdminLoginLayout";
    ViewData["Title"] = "عدم دسترسی";
}

<div class="login-page">

    <header class="login-topbar">
        <div class="login-portal-badge">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z" />
                <path d="m9 12 2 2 4-5" />
            </svg>
            <span>Admin Portal</span>
        </div>

        <div class="login-lang">
            <span>Vitorize</span>
        </div>
    </header>

    <main class="login-shell">

        <section class="login-hero">
            <div class="login-hero-content">
                <h1 class="login-brand-word">Vitorize</h1>

                <h2 class="login-hero-title">
                    دسترسی غیرمجاز
                </h2>

                <p class="login-hero-text">
                    این بخش فقط برای کاربران دارای نقش ادمین فعال است.
                </p>

                <div class="login-divider"></div>

                <img src="/images/admin-login-hero.svg"
                     class="login-hero-image"
                     alt="Access Denied" />
            </div>
        </section>

        <section class="login-form-zone">
            <div class="login-card">

                <img src="/images/logo.png"
                     class="login-logo"
                     alt="Vitorize Logo" />

                <h1 class="login-card-brand">Vitorize</h1>
                <h2 class="login-card-title">اجازه دسترسی ندارید</h2>

                <p class="login-card-subtitle">
                    حساب شما مجوز ورود به پنل مدیریت را ندارد.
                </p>

                <a href="/Admin/Auth/Login"
                   class="login-submit"
                   style="display:flex;align-items:center;justify-content:center;">
                    بازگشت به ورود ادمین
                </a>

                <div class="login-secure-note">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10Z" />
                        <path d="m9 12 2 2 4-5" />
                    </svg>

                    <span>فقط کاربران دارای دسترسی ادمین مجاز هستند</span>
                </div>
            </div>
        </section>

    </main>
</div>